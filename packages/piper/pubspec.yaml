name: piper
description: "Lifecycle-aware state management for Dart. ViewModels, async state, stream bindings, and task cancellation."
version: 0.1.0
license: MIT
homepage: https://github.com/glennsonna/piper
repository: https://github.com/glennsonna/piper
issue_tracker: https://github.com/glennsonna/piper/issues

environment:
  sdk: ^3.0.0

resolution: workspace

dependencies:
  meta: ^1.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
