name: flutter_piper
description: Flutter widgets for Piper State with lifecycle-aware ViewModel scopes, automatic dependency tracking, builders, listeners, and effects.
version: 0.1.0
repository: https://github.com/theGlenn/piper
homepage: https://glennso.dev/piper
issue_tracker: https://github.com/theGlenn/piper/issues
documentation: https://glennso.dev/piper/guide/view-model-scope

topics:
  - state-management
  - viewmodel
  - reactive-programming
  - widgets
  - flutter

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.6.0
  flutter: ">=3.24.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  piper_state: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
