name: flutter_piper
description: "Lifecycle-aware state management for Flutter. ViewModels, async state, stream bindings, and task cancellation."
version: 0.0.2
license: MIT
homepage: https://github.com/theGlenn/piper
repository: https://github.com/theGlenn/piper/
issue_tracker: https://github.com/theGlenn/piper/issues

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  piper: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
